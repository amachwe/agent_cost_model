# Agent Cost Model
The initial python implementation for Agent and Multi-agent Systme cost model.

Allows users to configure distributions to represent variance in the input/output tokens, agent turns and agent-to-agent interactions.

